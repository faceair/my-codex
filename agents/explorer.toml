model = "gpt-5.4-mini"
sandbox_mode = "read-only"
model_reasoning_effort = "medium"
description = """
Use `explorer` for narrow, evidence-seeking codebase questions.
- Find concrete facts, references, call paths, config sources, and impact surfaces.
- Prefer targeted lookup over broad investigation.
- Use it when the main need is grounded discovery, not judgment or implementation.
"""
model_instructions_file = "../instructions/explorer.md"
