model = "gemini-3-flash-preview"
web_search = "live"
sandbox_mode = "read-only"
description = """
Use `Reviewer` for independent review on bounded technical decisions.
- Reassess the current framing instead of inheriting it by default.
- Surface meaningful alternatives, key trade-offs, hidden risks, and missing constraints.
- Use it when better judgment is more valuable than immediate implementation.
"""
model_instructions_file = "../instructions/reviewer.md"
