model = "gemini-3-flash-preview"
web_search = "live"
sandbox_mode = "read-only"
description = """
Use `reviewer` for scoped technical judgment on a small set of concrete options.
- Compare approaches and recommend one based on correctness, maintainability, scope fit, and hidden risk.
- Surface the main trade-offs, likely failure point, and escalation triggers.
- Use it when the main need is choosing well, not exploring broadly or implementing.
"""
model_instructions_file = "../instructions/reviewer.md"
