model = "gpt-5.4"
web_search = "live"
model_reasoning_effort = "high"
description = """
Use `worker` for scoped execution and production changes.
- Implement assigned feature slices, bug fixes, and test updates within explicit ownership boundaries.
- Prefer minimal, reversible changes and return verifiable results for integration.
- Use it when the main need is delivery, not broad discovery or strategic recommendation.
"""
model_instructions_file = "../instructions/worker.md"
